# Interview Practice Studio — runtime dependencies.
#
# pyproject.toml is the source of truth ([project.dependencies]); this file
# mirrors those RUNTIME pins for `pip install -r requirements.txt` convenience.
# Keep the two consistent when changing a pin.
#
# Test/development tools live under pyproject's optional-dependencies and are
# installed separately with:  pip install -e ".[dev]"
#
# Deliberately minimal: no LangChain, no vector databases, no agents.

streamlit>=1.40,<2.0
httpx>=0.27,<1.0
pydantic>=2.9,<3.0
python-dotenv>=1.0,<2.0
pandas>=2.2,<3.0
openpyxl>=3.1,<4.0
