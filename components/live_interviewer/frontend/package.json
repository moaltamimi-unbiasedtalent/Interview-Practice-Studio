{
  "name": "live-interviewer",
  "version": "0.1.0",
  "private": true,
  "description": "Streamlit component: real-time Gemini Live interviewer (audio + WebSocket).",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@google/genai": "^0.14.0",
    "streamlit-component-lib": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}
