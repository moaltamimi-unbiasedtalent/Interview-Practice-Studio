{
  "name": "interview-studio-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright browser E2E for Interview Practice Studio (run locally against a running app).",
  "scripts": {
    "test": "playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.0"
  }
}
